<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <title>Dark horse</title>
</head>

<body>
    <div class="container w-75 vh-100 d-flex flex-column align-items-center justify-content-center"
        style="max-width: 380px;">
        <p class="fs-5 fw-semibold">다크호스 레슨신청</p>
        <% if(message) { %>
            <div class="alert alert-danger w-100"><%=message%></div>
        <% } %>
        
        <div class="w-100 d-flex flex-column mt-3 bg-body-secondary align-items-center">
            <form action="/user/login" method="POST"
                class="w-75 d-flex flex-column justify-content-center align-items-center">
                <label class="form-label mt-4 w-100">이름</label>
                <input class="form-control" type="text" name="name" value="<%=name%>" required>
                <label class="form-label mt-3 w-100">비밀번호</label>
                <input class="form-control" type="password" name="password" required>
                <button class="btn btn-primary d-block mx-auto mt-3 mb-3" type="submit">로그인</button>
            </form>
        </div>
    </div>
    <script src="/js/bootstrap.min.js"></script>
</body>

</html>